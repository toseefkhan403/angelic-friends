name: sponsor_a_dog
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+3

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Design system fonts (core/theme/app_theme.dart)
  google_fonts: ^6.2.1

  # Neobrutalist UI components (buttons, text fields, cards)
  brutalist_ui: ^0.0.1

  # Icon set used throughout the app instead of Material Icons
  lucide_icons_flutter: ^3.1.15

  # State management
  flutter_bloc: ^9.1.1
  bloc: ^9.0.1

  # Immutable models / unions
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0

  # Functional error handling (Either/Failure)
  dartz: ^0.10.1
  equatable: ^2.0.7

  # Networking
  dio: ^5.9.0
  connectivity_plus: ^7.0.0

  # Supabase
  supabase_flutter: ^2.10.4

  # Google / Apple native sign-in, fed into supabase.auth.signInWithIdToken
  google_sign_in: ^7.2.0
  sign_in_with_apple: ^8.1.0
  crypto: ^3.0.6

  # Opening external links (e.g. Google Maps for a shelter's location)
  url_launcher: ^6.3.1

  # Firebase (Analytics + Crashlytics — backend is Supabase)
  firebase_core: ^4.1.1
  firebase_analytics: ^12.1.1
  firebase_crashlytics: ^5.2.7

  # Dog detail gallery video playback
  video_player: ^2.13.0

  # RevenueCat SDK (Silver/Gold sponsorship paywalls)
  purchases_flutter: ^10.8.0
  purchases_ui_flutter: ^10.9.1

  # Disk-caches remote images so repeat views don't re-download
  cached_network_image: ^3.4.1

  # Shimmer loading placeholders (dog card image + explore feed skeleton)
  shimmer: ^4.0.0

  # Confetti burst on the post-sponsorship celebration dialog
  confetti: ^0.8.0

  # Native share sheet (dog detail page's "Share" button)
  share_plus: ^13.3.0

  # On-device storage for local-only settings (notification preferences)
  shared_preferences: ^2.5.5

  # Admin: pick a video from the gallery for weekly dog updates
  image_picker: ^1.1.2

  # Admin: transcode picked videos before upload — raw phone camera exports
  # (4K HEVC at 60+ Mbps) exceed what most Android hardware decoders can
  # play back, which is what left sponsors' video_player stuck on a spinner.
  video_compress: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Code generation
  build_runner: ^2.9.0
  freezed: ^3.2.3
  json_serializable: ^6.11.1

  # App icon generation (dev-only — run `dart run flutter_launcher_icons`)
  flutter_launcher_icons: ^0.14.4

  # Native splash screen generation (dev-only — run
  # `dart run flutter_native_splash:create`)
  flutter_native_splash: ^2.4.6

# flutter_launcher_icons config: source art lives in assets/icon/ (not
# bundled as a runtime asset — only read at generation time).
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"

# flutter_native_splash config: same white-background logo as the app icon.
# Android 12+'s splash API draws its own circular icon backdrop, so it needs
# the transparent-background foreground image (not the white-square one).
flutter_native_splash:
  color: "#FFFFFF"
  image: "assets/icon/app_icon.png"
  android_12:
    image: "assets/icon/app_icon_foreground.png"
    icon_background_color: "#FFFFFF"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
